@model IEnumerable<PhotoStudio.Data.tbl_ChargTyperef>

@{
    ViewBag.Title = "ViewChargeTypes";
}

<div class="col-md-12 col-sm-12 header-line mb-5 header-line text-center bgimage pb-10" style="min-height:70vh ">
    <div class="col-sm-12  col-md-12 mt-3 mb-4">
        <h2>ChargeType Details</h2>
    </div>

    <div class="col-md-11 col-sm-12 row mb-5 form-background pt-4 grid-padding" style="text-align: -webkit-left">
        <div class="col-12 p-0 mb-3">
            <a class="btn btn-success pull-right" href="@Url.Action("Create New", "Create")"><i class="fa fa-plus" aria-hidden="true"></i> Add New ChargeType</a>

        </div>
        <div class="col-12 p-0">
            <table class="table table-striped table-hover">
                <tr>
                    <th>
                        @*@Html.DisplayNameFor(model => model.ChargeTypeCode)*@
                        Charge Type Code
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Discription)
                    </th>
                    <th>
                        @*@Html.DisplayNameFor(model => model.EventType)*@
                        Event Type
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Price)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td class="pl-4">
                            @Html.DisplayFor(modelItem => item.ChargeTypeCode)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Discription)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.EventType)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Price)
                        </td>
                        @*<td>
                                @Html.ActionLink("Modify", "Edit", new { /* id=item.PrimaryKey */ }) |
                                @Html.ActionLink("View Details", "Details", new { /* id=item.PrimaryKey */ }) |
                                @Html.ActionLink("Remove", "Delete", new { /* id=item.PrimaryKey */ })
                            </td>*@
                        <td class="text-right">
                            <a class="btn btn-secondary mr-1" href="@Url.Action("Modify", "Edit", new {  /*id=item.PrimaryKey */})"><i class="fa fa-pencil" aria-hidden="true"></i> Edit</a>
                            |
                            <a class="btn btn-info ml-1 mr-1" href="@Url.Action("View Details", "Details", new { /* id=item.PrimaryKey */ })"><i class="fa fa-file-text-o" aria-hidden="true"></i> Details </a>
                            |
                            <a class="btn btn-danger ml-1" href="@Url.Action("Remove", "Remove", new { /* id=item.PrimaryKey */ })"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</a>
                        </td>
                    </tr>
                }


            </table>
        </div>

    </div>
</div>
<style>
    .bgimage {
        background-image: url('../../Images/login-bg.png');
        background-size: cover;
        background-attachment: fixed;
    }
</style>
